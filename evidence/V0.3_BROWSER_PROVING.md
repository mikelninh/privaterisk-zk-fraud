# PrivateRisk V0.3 — Browser PLONK Proving Evidence

## Claim
PrivateRisk can generate the `BALANCE_GT_TRANSFER` PLONK proof **on demand in a real Chromium browser** from the production bundle while keeping the synthetic balance out of the fraud/policy layer and out of public ledger state.

## Canonical case
- transfer threshold: `€15,000`
- synthetic private balance: `€27,000`
- predicate: `BALANCE_GT_TRANSFER`
- final network state: **NOT SUBMITTED**

## Production-build evidence
CI run: **34947398290**

The run successfully:
1. installs Compact `0.31.1`,
2. compiles the V0.3 Compact circuit,
3. installs the browser stack,
4. passes web/unit tests,
5. stages PLONK proving/verifying keys, ZKIR and SRS slices,
6. builds the production Vite bundle,
7. validates required proving assets in `dist/`,
8. launches the production preview in headless Chromium,
9. clicks `Evaluate + prove`,
10. waits for a fresh `✓ PROOF GENERATED` result,
11. confirms the private-input UI says `WITHHELD`,
12. confirms the network boundary says `NOT SUBMITTED`,
13. fails the smoke test on browser/page errors or proof-runtime failure states.

Result: **PASS**.

## Cryptographic regression evidence
Midnight Contract run: **34947398287**

The dedicated contract workflow keeps the Compact simulator and real PLONK checker path green independently of the browser UI.

## Truth boundary
V0.3 proves the browser/client proving path and local constraint verification. It does **not** yet prove production readiness for a bank and it does **not** claim Midnight Preprod/Mainnet submission, settlement, finality, external attestor trust, production key custody, regulatory approval, or live fraud-model performance.

Those are the next production-hardening layers.
